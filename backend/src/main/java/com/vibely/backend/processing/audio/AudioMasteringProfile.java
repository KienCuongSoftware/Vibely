package com.vibely.backend.processing.audio;

/**
 * FFmpeg mastering preset for uploaded UGC — tuned for mobile playback (TikTok / Reels style).
 */
public enum AudioMasteringProfile {
    SPEECH,
    MUSIC,
    CINEMATIC,
    DEFAULT
}
