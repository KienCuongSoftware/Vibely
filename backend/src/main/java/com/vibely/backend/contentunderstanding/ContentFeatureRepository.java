package com.vibely.backend.contentunderstanding;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ContentFeatureRepository extends JpaRepository<ContentFeatureEntity, Long> {
}
