# Studio Analytics

## API

- `GET /api/studio/analytics/overview`
- `GET /api/studio/analytics/video/{publicId}`

## Metrics exposed

- Views, watch time aggregates
- Per-video breakdown for creator's content

## Frontend

`StudioPage`, `StudioVideoPage` — charts and tables (verify components in `pages/Studio*`).

## Future

- Audience demographics
- Traffic sources (share links)
- Export CSV
